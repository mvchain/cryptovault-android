<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--定义seekbar滑动条的底色-->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="7dp" />
            <gradient
                android:angle="270"
                android:centerColor="@color/toge_gray"
                android:centerY="0.75"
                android:endColor="@color/toge_gray"
                android:startColor="@color/toge_gray" />
        </shape>
    </item>
    <!--定义seekbar滑动条进度颜色-->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="7dp"/>
                <solid android:color="@color/login_content"/>
            </shape>
        </clip>
    </item>
</layer-list>